spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            - ${CONFIG_REPO_LOCATION:file:./config-repo}
            - ${CONFIG_REPO_LOCATION:file:../config-repo}

server:
  port: 8071
